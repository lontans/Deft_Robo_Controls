# Controls PCB — progress summary (25 Jul 2026)

**Baseline:** `7203a0129370666f995bcb071285e7486318aab1`  
*(2026-07-23 — “jetson uart pdu test script, optimized 200Hz full loop, cubemars+api level changes”)*

**HEAD at writing:** `ddec6d8` (mouse teleop + per-joint KD + continuous `--mouse`; GitHub tip after rewrite).  
**Prior checkpoint this doc:** `ce7b2f4` (dashboard / MI / docs).

---

## How much changed since the baseline

**Through `ce7b2f4`:** **7 commits**, roughly **+23k / −3k lines across ~238 files** — a large jump from “PDU UART + 200 Hz plant loop tuning” into a full bench ops stack.

| Commit | Theme |
|--------|--------|
| `708795c` | PDU state machine tested; deft_vbeta API layer testing |
| `8858f37` | Single-arm full bench test |
| `c5ccb0d` | Continuous bringup; mixed-protocol bus 4–6; host debug dashboard live |
| `e25df58` | Continuous single YAM arm + PDU soft-kill complete |
| `f7346b5` | Repo cleanup; new docs; Soft-DFU setup |
| `290ee1f` | deft_vbeta working tree; instruction manual; dashboard changes |
| `ce7b2f4` | Dashboard improvements; docs; Mission Impossible limit testing |
| `4391721` | Summary docs update |
| `ddec6d8` | Mouse teleop → continuous; per-joint KD; Quest/mouse ingress helpers; gravity_comp module |

### Firmware / plant (through `ce7b2f4`)

- PDU V/I soft-kill overlay (`pdb_vi_limits`, host + FW alignment).
- Soft-DFU path hardened in docs/code; Jetson USB-DFU caveat understood (option bytes / `nBOOT0` brick if leave never runs).
- MCP CH4–6 + FDCAN mixed-protocol plant path exercised under continuous load.
- LED / plant command / actuator maintain paths touched as part of continuous + PDU work.

### Host SDK & drivers

- **`yam_continuous_all.py`** — progressive MIT latch, J2 CLEAR cruise **or** `--mouse`, base spin, DXL present/cruise, soft-kill park; Damiao REG_SCAN discover **opt-in** (`--discover` / `--discover-base`).
- **`mouse_arm_teleop.py` / `mouse_teleop.py`** — Logitech M650 → left Damiao (CDC) + TeleopSample/RTC input shape.
- **`quest_udp_sniff.py` / `quest_openxr_teleop.py`** — Quest ingress helpers (Link not viable on this laptop).
- **vbeta:** per-joint `DEFAULT_ARM_KD`, `gravity_comp.py` (opt-in `--gravity-comp`), arm/teleop/dashboard KD consumers.
- Bench helpers, PDU UART sim, Mission Impossible, debug dashboard (as before).

### Docs & ops

- Instruction book TOC: `docs/manual/README.md`
- Operator exercises: `docs/user-tutorial.md`
- Per-peripheral manuals under `docs/peripherals/`
- `docs/i2rt-vs-ours-arm-compare.md`, `docs/quest-pc-ingress.md`
- Bench: `bench-rs02-ch5-0x74-2026-07-25.md` (Claude-1 CH5 bringup)
- Mission Impossible findings: `mission_impossible_findings.md`

---

## Session since `ce7b2f4` (25 Jul evening) — what was proved

Three-track day: Claude-1 (CH5 `0x74`), Claude-2 (arm KD / i2rt compare), Cursor (Quest→mouse→continuous teleop). CDC exclusivity respected per plan.

### Claude-1 — CH5 RobStride `0x74` (closes earlier “silent” open)

See `bench-rs02-ch5-0x74-2026-07-25.md`.

| Gate | Result |
|------|--------|
| Solo `0x74` (0x70 removed) probe + `rs02_channel_bringup` | **PASS** — cali + 360° teleop, track peak ~0.014 rad |
| Prior silence root cause | Daisy contention with `0x70`, not dead ID/power |
| Dual CH5 after `0x70` reattach (sibling-safe reset → enable-only probe) | **PASS** — both `found=1` |
| Connector flake (40× probe) | **PASS** — 40/40; no static intermittent |
| Dual hold with soft gain ramp | **PASS** — peak err &lt;4 mrad both slots |

Open nuance: static probe ≠ load/vibration; longer continuous dual-RS under motion still the stress check if connector doubt remains. VBUS ~23.8 V warning on cali (non-blocking).

### Claude-2 — arm controls (synced to Jetson; Cursor exercised)

- Per-joint `DEFAULT_ARM_KD = (2.5, 3.75, 5.6, 3.75, 1.5, 1.5, 1.25)` (was flat `1.0`).
- Damiao semantics clarified: **`fault==1` = MIT green**; `0` = not latched; `(fault&0xF)>=8` = hard.
- `gravity_comp` module present; continuous opt-in via `--gravity-comp` (default off; scale starts 0).
- Compare note: `docs/i2rt-vs-ours-arm-compare.md`.

### Cursor — Jetson latch smoke → laptop mouse → continuous `--mouse`

| Gate | Result |
|------|--------|
| Progressive latch + slow J1 jog, J2 frozen at hardstop FB (~−2.61) | **PASS** — no slam; Claude-2 KD loaded |
| `mouse_arm_teleop.py` on laptop CDC (M650 joystick) | **PASS** — latch all-7 green; J1/J2/J3/J4/J5/J7 mapping works |
| Middle = motion gate only (hold keeps streaming; no re-seed hold from FB) | **PASS** after fix (early re-seed caused droop) |
| Idle joints hard-freeze hold (no soft FB-track sag when switching axes) | **PASS** |
| No settle→FB on enable release (settle mid-lift caused J2 sag) | **PASS** after fix |
| `yam_continuous_all.py --mouse` full stack | **PASS** — arm teleop + base spin + DXL cruise; status showed live `dxl=cmd/fb`, s22–s25 tracking |
| Skip CH1 Damiao discover by default (trust CFG + FB) | Used successfully this session |
| Deep J2 CLEAR (~−4.5 hold) under mouse + gravity | Reached; late buzz observed once with always-on kp×1.4 + large lead |
| Anti-buzz experiment (hold kp×1.0, lead 0.18) | **REGRESSED** lift range / enable-up sag — **reverted** to kp×1.4 hold + lead 0.32 + faster rates (in `ddec6d8`) |
| PDB line `hard_estop/COMMS_LOSS` during healthy teleop | **Not a peripheral fault** when `estop_gpio=1` and arm/base/DXL still track — stale PDU UART overlay |

### Explicitly not proved / parked

- Quest Link / OpenXR on ThinkPad X1 — blocked (“not strong enough” / no runtime); mouse is the interim ingress.
- Gravity torque feedforward under real teleop — module exists, **not** calibrated/enabled in the successful mouse runs (J2 still fights gravity with kp boost).
- Product base IDs `0x01`/`0x02` remap.
- Soft-DFU on Jetson still BLOCKED.
- Long soak for late-session J2 buzz with restored boost (acceptable for now; FF is the real fix if it returns).

---

## Current state of the repo (bench truth)

### What is proven

- **Left YAM arm (CH1):** progressive MIT green → CLEAR cruise **and** interactive mouse teleop (standalone + continuous `--mouse`).
- **Base (bench map):** CH5 `0x70` **and** `0x74`, CH6 `0x75`, CH6 Damiao `0x06` — dual CH5 hold PASS; continuous spin observed under `--mouse`.
- **DXL neck:** present + cruise tracking under continuous mouse (cmd/fb moving in status lines) — improves on the earlier “present OK / goals dead” open from morning notes.
- **TX bandwidth / MI M1–M4 / soft-kill park:** as previously recorded under `ce7b2f4`.
- **Host teleop path:** M650 → `MouseArmRuntime` → plant desires; cornflower LED desire latched (PDB overlay may still blink red when UART stale).

### Open / flaky (updated end of 25 Jul)

1. **CH5 `0x74`** — morning “silent” **closed** for probe/hold; optional follow-up = dual-RS under sustained motion if connector still suspect. Details in `bench-rs02-ch5-0x74-2026-07-25.md`.
2. **DXL neck** — morning “torque goals don’t move” looked **regressed vs 07-24**; evening continuous `--mouse` showed live tracking again — treat morning note as session-specific unless it returns.
3. **J2 gravity** — kp×1.4 hold works for lift/hold; late buzz possible deep in CLEAR; **gravity FF** still the designed next step (`--gravity-comp` + calibrate), not another kp bump.
4. **PDU COMMS_LOSS LED / status** — confusing during laptop teleop without live PDB peer; peripherals can be fine.
5. Product base ID remap, right arm, Soft-DFU Jetson, lift CANopen — unchanged.

### Rough maturity

~**80–90%** through firmware/host bringup for this board: continuous + interactive mouse teleop are real on the full stack; remaining work is gravity FF / buzz polish, product ID map, flash/ops trust, and any dual-RS motion soak.

---

## How to run (pointers)

| Goal | Entry |
|------|--------|
| Instruction TOC | `docs/manual/README.md` |
| Exercise workbook | `docs/user-tutorial.md` |
| Continuous cruise (J2 CLEAR) | `python scripts/yam_continuous_all.py` |
| Continuous + mouse teleop | `python scripts/yam_continuous_all.py --mouse` |
| Arm-only mouse (laptop CDC) | `python scripts/mouse_arm_teleop.py` |
| Stress suite | `python scripts/mission_impossible.py` (Jetson CDC) |
| Findings | `mission_impossible_findings.md` |
| i2rt compare | `docs/i2rt-vs-ours-arm-compare.md` |
| API | `docs/api.md`, `scripts/deft_controls_sdk/README.md` |

**CDC rule:** one owner at a time (`yam_continuous`, dashboard Connect, MI, Soft-DFU). Dashboard follow mode is OK while continuous owns COM.

**Git note (ops):** GitHub `main` rewritten to `ddec6d8` (author-clean); GitLab may still carry prior tip `4b645e2` in history — prefer separate pushes (`origin` vs `old-origin`) until histories are intentionally reconciled. Do not `git add` `docs/deft_vbeta_ref/{deft_rtc,i2rt_cpp}/` or `scripts/_tmp_*`.

---

## Suggested next focus

1. Optional: dual CH5 continuous/motion soak if connector doubt remains; otherwise treat `0x74` as restored.
2. Enable/calibrate **`--gravity-comp`** on a short mouse lift (reduce reliance on J2 kp×1.4).
3. Product base ID remap when the physical map is ready.
4. Keep Soft-DFU prove on a host that enumerates DF11; Jetson = SWD recovery only for now.
5. Quest/OpenXR only if a capable PC + runtime appears — mouse path is the interim.
